result = [5.0,0.4]

def divider(a, b):
    if a < b:
        raise ValueError(f"ValueError: a ({a}) is less than b ({b})")
    if b > 100:
        raise IndexError(f"IndexError: b ({b}) is greater than 100")
    return a / b

data = {10: 2, 2: 5, "123": 4, 18: 0, 8: 4}  # Видалено некоректний ключ []

for key in data:
    try:
        # Перетворюємо ключ у число, якщо це можливо
        a = int(key) if isinstance(key, str) else key
        b = data[key]
        res = divider(a, b)
        result.append(res)
    except ZeroDivisionError:
        print(f"ZeroDivisionError: Division by zero for key={key}, value={data[key]}")
    except ValueError as ve:
        print(ve)
    except IndexError as ie:
        print(ie)
    except Exception as e:
        print(f"Unexpected error for key={key}, value={data[key]}: {e}")

print("Result:", result)
